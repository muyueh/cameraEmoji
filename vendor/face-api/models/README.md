# face-api.js model placeholder

Add the downloaded face-api.js model manifests and shard files to this directory
if you prefer to host them locally. See the parent `vendor/face-api/README.md`
for download commands.
